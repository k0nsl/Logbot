<!DOCTYPE html>
<html>
  <head>
    <title>k0nslIRC trunk | #<%= @channel %></title>
    <link rel="stylesheet" type="text/css" href="/assets/widget.css">
    <base target="_blank" />
<style>
/* body { background: black !important }
.body .logs li .msg { color: white !important; font-size: 22px !important }
*/
</style>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  </head>
  <body>
    <div class="wrapper">
      <div class="header">
        <h1>k0nslIRC trunk <span class="channel">#<%= @channel %></span></h1>
      </div>
      <div class="body">
        <div>
          <ul class="logs">
            <% @msgs.each.with_index do |m, i| %>
              <% index = @msgs.length - 1 - i %>
              <li id="<%= index %>">
                <a class="time" href="<%= "##{index}" %>" target="_self" title="<%= "##{index}" %>"><%= Time.at(m['time'].to_f).strftime('%T') %></a>
                <span class="nick"><%= user_nick(m) %></span>
                <span class="msg wordwrap"><%= user_text(m) %></span>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
      <%= erb :_footer %>
    </div>

    <script src="//cdn.k0nsl.org/js/jquery/jquery-1.8.3.min.js"></script>
    <script src="//cdn.k0nsl.org/js/emojione.min.js"></script>
    <link rel="stylesheet" href="//cdn.k0nsl.org/css/emojione.min.css"/>
    <script src="/assets/emoji.js"></script>

    <script>
      var channel = "<%= @channel %>";
    </script>
    <script src="/assets/live.js"></script>
    <script>
      pollNewMsg(true);
    </script>
  </body>
</html>
